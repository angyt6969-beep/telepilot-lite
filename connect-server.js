import http from "node:http";
import crypto from "node:crypto";
import { TelegramClient } from "teleproto";
import { StoreSession } from "teleproto/sessions/index.js";

const MAX_BODY = 8192;
const CONNECT_TTL_MS = 10 * 60_000;
const LOGIN_TTL_MS = 7 * 60_000;

function json(res, status, body, extraHeaders = {}) {
  res.writeHead(status, {"content-type":"application/json; charset=utf-8","cache-control":"no-store","x-content-type-options":"nosniff","referrer-policy":"no-referrer",...extraHeaders});
  res.end(JSON.stringify(body));
}
function html(res,status,body,nonce){
  res.writeHead(status,{"content-type":"text/html; charset=utf-8","cache-control":"no-store","x-content-type-options":"nosniff","referrer-policy":"no-referrer","permissions-policy":"camera=(), microphone=(), geolocation=()","content-security-policy":`default-src 'none'; style-src 'unsafe-inline'; script-src 'nonce-${nonce}'; connect-src 'self'; form-action 'self'; base-uri 'none'; frame-ancestors 'none'`});
  res.end(body);
}
async function readJson(req){const chunks=[];let total=0;for await(const c of req){total+=c.length;if(total>MAX_BODY)throw new Error("REQUEST_TOO_LARGE");chunks.push(c)}return chunks.length?JSON.parse(Buffer.concat(chunks).toString("utf8")):{};}
function parseCookies(req){const out={};for(const p of String(req.headers.cookie||"").split(";")){const i=p.indexOf("=");if(i>0)out[p.slice(0,i).trim()]=decodeURIComponent(p.slice(i+1).trim())}return out;}
function safeEqualHex(a,b){if(!/^[a-f0-9]{64}$/i.test(a||"")||!/^[a-f0-9]{64}$/i.test(b||""))return false;return crypto.timingSafeEqual(Buffer.from(a,"hex"),Buffer.from(b,"hex"));}
function cleanPhone(v){const p=String(v||"").trim().replace(/[\s()-]/g,"");return /^\+[1-9]\d{6,14}$/.test(p)?p:null;}
function humanize(err){const raw=String(err?.errorMessage||err?.message||"").toUpperCase();if(raw.includes("PHONE_CODE_INVALID"))return"That login code is incorrect. Try the newest code from Telegram.";if(raw.includes("PHONE_CODE_EXPIRED"))return"That login code expired. Start the login again.";if(raw.includes("PASSWORD_HASH_INVALID"))return"That 2-step verification password is incorrect.";if(raw.includes("PHONE_NUMBER_INVALID"))return"Telegram did not accept that phone number.";if(raw.includes("FLOOD"))return"Telegram temporarily limited login attempts. Please wait before trying again.";if(raw.includes("AUTH_USER_CANCEL"))return"Login cancelled.";return"Telegram could not complete that step. Please try again.";}

function pageTemplate(nonce){return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="theme-color" content="#111827"><title>TelePilot Connect</title><style>
:root{color-scheme:dark;font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}*{box-sizing:border-box}body{margin:0;min-height:100vh;background:linear-gradient(180deg,#0b1020,#111827 48%,#0b1020);color:#f8fafc;display:flex;align-items:center;justify-content:center;padding:24px}.wrap{width:min(100%,440px)}.brand{text-align:center;margin-bottom:20px}.logo{width:66px;height:66px;border-radius:22px;margin:0 auto 12px;background:linear-gradient(145deg,#60a5fa,#7c3aed);display:grid;place-items:center;font-size:34px}.brand h1{margin:0;font-size:27px}.brand p{margin:7px 0 0;color:#94a3b8;font-size:14px}.card{background:#111827e8;border:1px solid #ffffff17;border-radius:24px;padding:22px}.step{display:none}.step.active{display:block}h2{font-size:21px;margin:0 0 8px}.sub{color:#aeb9ca;line-height:1.45;margin:0 0 18px;font-size:15px}.eyebrow{font-size:12px;letter-spacing:.08em;text-transform:uppercase;color:#7f8da3;margin-bottom:8px}label{display:block;font-size:13px;color:#cbd5e1;margin:0 0 8px}input{width:100%;font-size:18px;padding:15px 16px;border-radius:14px;border:1px solid #ffffff22;background:#070b14;color:#fff;outline:none}button{width:100%;border:0;border-radius:14px;padding:15px 16px;margin-top:13px;font-size:16px;font-weight:700;color:#fff;background:linear-gradient(90deg,#3b82f6,#7c3aed)}.ghost{background:transparent;color:#94a3b8;border:1px solid #ffffff17}.secondary{background:#1e293b}.status{min-height:21px;margin-top:12px;color:#fca5a5;font-size:14px}.hint{font-size:12px;color:#7f8da3;margin-top:14px;line-height:1.45}.success{text-align:center}.ok{font-size:52px}.spinner{width:34px;height:34px;border:3px solid #ffffff20;border-top-color:#60a5fa;border-radius:50%;animation:s 1s linear infinite;margin:8px auto 18px}@keyframes s{to{transform:rotate(360deg)}}.passwordWrap{position:relative}.passwordWrap input{padding-right:72px}.showPass{position:absolute;right:8px;top:8px;width:auto;margin:0;padding:8px 10px;font-size:13px;background:#1e293b}
</style></head><body><div class="wrap"><div class="brand"><div class="logo">✈️</div><h1>TelePilot Connect</h1><p>Connect your Telegram account</p></div><div class="card">
<section id="phone" class="step active"><div class="eyebrow">Step 1</div><h2>Connect your Telegram</h2><p class="sub">Enter the phone number on the account you want TelePilot to use.</p><label>Phone number with country code</label><input id="phoneInput" type="tel" autocomplete="tel" inputmode="tel" placeholder="+___ __________"><button id="phoneBtn">Continue</button><div class="status" id="phoneStatus"></div><div class="hint">Start with + and your country code.</div></section>
<section id="waiting" class="step"><div class="spinner"></div><h2 style="text-align:center">Connecting to Telegram…</h2><p class="sub" style="text-align:center">This normally takes a few seconds.</p><button class="ghost cancelLogin">Cancel</button></section>
<section id="code" class="step"><div class="eyebrow">Step 2</div><h2>Enter your Telegram code</h2><p class="sub" id="codeSub">Enter the newest login code from Telegram.</p><input id="codeInput" type="text" autocomplete="one-time-code" inputmode="numeric" maxlength="12" placeholder="12345"><button id="codeBtn">Verify code</button><button class="ghost cancelLogin">Cancel</button><div class="status" id="codeStatus"></div></section>
<section id="password" class="step"><div class="eyebrow">Final security check</div><h2>2-step verification</h2><p class="sub" id="passwordSub">This account has 2FA enabled.</p><div class="passwordWrap"><input id="passwordInput" type="password" autocomplete="off" placeholder="Password"><button class="showPass" id="showPass" type="button">Show</button></div><button id="passwordBtn">Continue</button><button class="ghost cancelLogin">Cancel</button><div class="status" id="passwordStatus"></div></section>
<section id="email" class="step"><div class="eyebrow">Telegram security check</div><h2>Email verification</h2><p class="sub">Telegram requires an email address for this login.</p><input id="emailInput" type="email" autocomplete="email" placeholder="you@example.com"><button id="emailBtn">Continue</button><button class="ghost cancelLogin">Cancel</button><div class="status" id="emailStatus"></div></section>
<section id="email_code" class="step"><div class="eyebrow">Telegram security check</div><h2>Check your email</h2><p class="sub" id="emailCodeSub">Enter the verification code Telegram sent.</p><input id="emailCodeInput" type="text" autocomplete="one-time-code" inputmode="numeric" maxlength="16" placeholder="Code"><button id="emailCodeBtn">Verify email</button><button class="ghost cancelLogin">Cancel</button><div class="status" id="emailCodeStatus"></div></section>
<section id="done" class="step success"><div class="ok">✅</div><h2>Account connected</h2><p class="sub" id="doneText">You can return to TelePilot.</p><button class="secondary" id="closeBtn">Return to Telegram</button></section>
<section id="failed" class="step"><h2>Couldn’t connect</h2><p class="sub" id="failedText">Please try again.</p><button class="secondary" id="retryBtn">Try again</button><button class="ghost returnTelegram">Return to Telegram</button></section>
</div></div><script nonce="${nonce}">
const qs=new URLSearchParams(location.search),connect={uid:qs.get('uid'),exp:qs.get('exp'),sig:qs.get('sig')};let current='phone',pollTimer=null;const $=id=>document.getElementById(id);function show(id){document.querySelectorAll('.step').forEach(x=>x.classList.remove('active'));$(id).classList.add('active');current=id}async function post(url,body){const r=await fetch(url,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||'Request failed');return j}function startPolling(){if(pollTimer)clearInterval(pollTimer);pollTimer=setInterval(checkStatus,900);checkStatus()}async function checkStatus(){try{const r=await fetch('/api/login/status',{cache:'no-store'}),s=await r.json();if(!r.ok)return;if(s.step==='code'){show('code');$('codeSub').textContent=s.viaApp?'Telegram sent the code inside your Telegram app. Enter the newest code below.':'Telegram sent a login code. Enter it below.';if(s.error)$('codeStatus').textContent=s.error}else if(s.step==='password'){show('password');$('passwordSub').textContent=s.hint?('2FA hint: '+s.hint):'This account has 2-step verification enabled.';if(s.error)$('passwordStatus').textContent=s.error}else if(s.step==='email'){show('email');if(s.error)$('emailStatus').textContent=s.error}else if(s.step==='email_code'){show('email_code');$('emailCodeSub').textContent=s.emailPattern?('Telegram sent a code to '+s.emailPattern+'.'):'Telegram sent a verification code to your email.';if(s.error)$('emailCodeStatus').textContent=s.error}else if(s.step==='done'){clearInterval(pollTimer);show('done');$('doneText').textContent=s.username?('Connected as @'+s.username+'. You can return to TelePilot.'):'Your Telegram account is connected.'}else if(s.step==='failed'){clearInterval(pollTimer);show('failed');$('failedText').textContent=s.error||'Telegram could not complete the login.'}else if(!['phone','waiting'].includes(current))show('waiting')}catch{}}
$('phoneBtn').onclick=async()=>{try{await post('/api/login/start',{...connect,phone:$('phoneInput').value.trim()});show('waiting');startPolling()}catch(e){$('phoneStatus').textContent=e.message}};async function submit(kind,input,status){try{await post('/api/login/input',{kind,value:$(input).value});$(input).value='';show('waiting')}catch(e){$(status).textContent=e.message}}$('codeBtn').onclick=()=>submit('code','codeInput','codeStatus');$('passwordBtn').onclick=()=>submit('password','passwordInput','passwordStatus');$('emailBtn').onclick=()=>submit('email','emailInput','emailStatus');$('emailCodeBtn').onclick=()=>submit('email_code','emailCodeInput','emailCodeStatus');$('showPass').onclick=()=>{const i=$('passwordInput'),showing=i.type==='password';i.type=showing?'text':'password';$('showPass').textContent=showing?'Hide':'Show'};document.querySelectorAll('.cancelLogin').forEach(b=>b.onclick=async()=>{try{await post('/api/login/cancel',{})}catch{}location.href='tg://resolve?domain=TelePilottBot'});document.querySelectorAll('.returnTelegram').forEach(b=>b.onclick=()=>location.href='tg://resolve?domain=TelePilottBot');$('retryBtn').onclick=()=>location.reload();$('closeBtn').onclick=()=>location.href='tg://resolve?domain=TelePilottBot';if(!connect.uid||!connect.exp||!connect.sig){show('failed');$('failedText').textContent='This connection link is invalid or expired.'}
</script></body></html>`;}

export function createConnectService({botToken,apiId,apiHash,getSessionName,publicUrl,onConnected}){
  const pending=new Map();
  function signature(uid,exp){return crypto.createHmac("sha256",botToken).update(`${uid}.${exp}.telepilot-connect-v2`).digest("hex")}
  function makeConnectUrl(uid){const exp=Date.now()+CONNECT_TTL_MS,sig=signature(uid,exp);return `${publicUrl}/connect?uid=${encodeURIComponent(uid)}&exp=${exp}&sig=${sig}`}
  function verify(uid,expRaw,sig){const exp=Number(expRaw);if(!/^\d+$/.test(String(uid||""))||!Number.isFinite(exp))return false;if(Date.now()>exp||exp-Date.now()>CONNECT_TTL_MS+30000)return false;return safeEqualHex(signature(uid,exp),String(sig||""))}
  function getLogin(req){const id=parseCookies(req).tp_login;return id?pending.get(id)||null:null}
  function waitForInput(login,kind,meta={}){login.step=kind;login.error=login.nextError||null;login.nextError=null;Object.assign(login,meta);return new Promise((resolve,reject)=>{login.waiting={kind,resolve,reject}})}
  function stopLogin(login,message){if(login.finished)return;login.finished=true;login.step="failed";login.error=message;if(login.waiting){login.waiting.reject(new Error("AUTH_USER_CANCEL"));login.waiting=null}void login.client.disconnect().catch(()=>{})}
  async function startAuth(login,phone){try{await login.client.start({phoneNumber:phone,phoneCode:async viaApp=>waitForInput(login,"code",{viaApp:!!viaApp}),password:async hint=>waitForInput(login,"password",{hint:hint||""}),emailAddress:async()=>waitForInput(login,"email"),emailVerification:async(options={})=>({type:"code",code:await waitForInput(login,"email_code",{emailPattern:options.emailPattern||""})}),firstAndLastNames:async()=>{throw new Error("AUTH_USER_CANCEL")},onError:async err=>{const raw=String(err?.errorMessage||err?.message||"").toUpperCase();login.nextError=humanize(err);return raw.includes("FLOOD")||raw.includes("PHONE_CODE_EXPIRED")||raw.includes("AUTH_USER_CANCEL")}});if(login.finished)return;const me=await login.client.getMe();login.finished=true;login.step="done";login.username=me?.username||"";login.waiting=null;await onConnected(login.uid,login.client,me)}catch(err){if(login.finished&&login.step==="failed")return;login.finished=true;login.step="failed";login.error=login.nextError||humanize(err);login.waiting=null;try{await login.client.disconnect()}catch{}}}
  const server=http.createServer(async(req,res)=>{try{const url=new URL(req.url||"/",publicUrl);if(req.method==="GET"&&url.pathname==="/")return json(res,200,{ok:true,service:"TelePilot Connect"});if(req.method==="GET"&&url.pathname==="/connect"){const uid=url.searchParams.get("uid"),exp=url.searchParams.get("exp"),sig=url.searchParams.get("sig"),nonce=crypto.randomBytes(18).toString("base64");if(!verify(uid,exp,sig))return html(res,403,pageTemplate(nonce),nonce);return html(res,200,pageTemplate(nonce),nonce)}if(req.method==="POST"&&url.pathname==="/api/login/start"){const body=await readJson(req);if(!verify(body.uid,body.exp,body.sig))return json(res,403,{error:"This connection link expired. Open TelePilot and try again."});const phone=cleanPhone(body.phone);if(!phone)return json(res,400,{error:"Enter your full phone number starting with + and the country code."});for(const l of pending.values())if(String(l.uid)===String(body.uid)&&!l.finished)stopLogin(l,"A newer login attempt was started.");const id=crypto.randomBytes(32).toString("hex");const sessionName=await getSessionName(String(body.uid));const client=new TelegramClient(new StoreSession(sessionName),apiId,apiHash,{connectionRetries:5});const login={id,uid:String(body.uid),client,step:"starting",error:null,nextError:null,waiting:null,finished:false,username:""};pending.set(id,login);setTimeout(()=>{if(!login.finished)stopLogin(login,"This login attempt expired. Start again from TelePilot.");setTimeout(()=>pending.delete(id),60000)},LOGIN_TTL_MS);void startAuth(login,phone);return json(res,200,{ok:true},{"set-cookie":`tp_login=${id}; Path=/; HttpOnly; Secure; SameSite=Strict; Max-Age=${Math.ceil(LOGIN_TTL_MS/1000)}`})}if(req.method==="GET"&&url.pathname==="/api/login/status"){const l=getLogin(req);if(!l)return json(res,404,{error:"No active login."});return json(res,200,{step:l.step,error:l.error,viaApp:!!l.viaApp,hint:l.hint||"",emailPattern:l.emailPattern||"",username:l.username||""})}if(req.method==="POST"&&url.pathname==="/api/login/input"){const l=getLogin(req);if(!l||l.finished)return json(res,409,{error:"This login is no longer active."});const body=await readJson(req),kind=String(body.kind||""),raw=String(body.value??""),value=kind==="password"?raw:raw.trim();if(!l.waiting||l.waiting.kind!==kind)return json(res,409,{error:"Telegram is not waiting for that step yet."});if(!value||value.length>256)return json(res,400,{error:"Enter a valid value."});const {resolve}=l.waiting;l.waiting=null;l.step="working";l.error=null;resolve(value);return json(res,200,{ok:true})}if(req.method==="POST"&&url.pathname==="/api/login/cancel"){const l=getLogin(req);if(l&&!l.finished)stopLogin(l,"Login cancelled.");return json(res,200,{ok:true},{"set-cookie":"tp_login=; Path=/; HttpOnly; Secure; SameSite=Strict; Max-Age=0"})}return json(res,404,{error:"Not found"})}catch(err){console.error("TelePilot web service error:",err?.message||err);if(String(err?.message||"")==="REQUEST_TOO_LARGE")return json(res,413,{error:"Request too large"});return json(res,500,{error:"TelePilot hit an unexpected error."})}});
  return {makeConnectUrl,listen(port){server.listen(port,"0.0.0.0",()=>console.log(`TelePilot Connect listening on port ${port}`))}};
}
